<h1>Exercise Index</h1>

<h3><%= link_to "New Exercise", { :controller => "exercises", :action => "new" } %></h3>

<h2>Exercise List:</h2>

<% @exercises.each do |exercise| %>
    <%= link_to exercise.name, exercise_path(exercise) %>, (<%= exercise.user_id %>) <br>
<% end %>